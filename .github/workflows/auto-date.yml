name: Auto date for wiki MD

on:
  push:
    branches: ["**"]
    paths: ["wiki/**/*.md"]
  pull_request:
    branches: ["**"]
    paths: ["wiki/**/*.md"]

permissions:
  contents: write

jobs:
  update-dates:
    if: ${{ !contains(github.event.head_commit.message, '[auto-date]') && !contains(github.event.head_commit.message, '[no-auto-date]') }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Ensure updater exists
        run: |
          test -f scripts/update_wiki_dates.py || { echo "scripts/update_wiki_dates.py not found"; exit 1; }
          chmod +x scripts/update_wiki_dates.py

      - name: Determine changed files
        run: |
          set -e
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch origin ${{ github.base_ref }} ${{ github.head_ref }}
            git diff --name-only "origin/${{ github.base_ref }}...origin/${{ github.head_ref }}" -- 'wiki/**/*.md' > changed_files.txt
          else
            before="${{ github.event.before }}"
            after="${{ github.sha }}"
            git diff --name-only "$before" "$after" -- 'wiki/**/*.md' > changed_files.txt
          fi
          cat changed_files.txt

      - name: Run updater
        run: python scripts/update_wiki_dates.py

      - name: Check for changes
        id: changed
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Start ssh-agent
        if: steps.changed.outputs.changed == 'true'
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.AUTO_COMMIT_BOT_DEPLOY }}

      - name: Add github.com to known_hosts
        if: steps.changed.outputs.changed == 'true'
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -t rsa,ecdsa,ed25519 github.com >> ~/.ssh/known_hosts

      - name: Commit and push via SSH (deploy key bypass)
        if: steps.changed.outputs.changed == 'true'
        run: |
          set -e
          git config user.name "auto-date"
          git config user.email "auto-date@users.noreply.github.com"
          git add ./wiki/
          git commit -m "[auto-date] Update wiki dates"
          git remote set-url origin git@github.com:S-P-F-Base/spf-base-site.git
          git push origin HEAD:master
